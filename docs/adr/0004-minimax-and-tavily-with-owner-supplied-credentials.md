# Use MiniMax and Tavily with Owner-supplied credentials

The first runtime will use global MiniMax Token Plan access for `MiniMax-M2.7` and Tavily for web discovery, with both Credentials supplied and controlled by the Owner. Agent Base will encrypt the Credentials and isolate both services behind provider boundaries, accepting the operational dependency in exchange for predictable local ownership while deferring general provider selection and arbitrary Connectors.
