# Preserve immutable Run attempts and human gates

A Task is durable, while each Run and its Report are immutable records of one attempt; revision feedback creates another Run rather than mutating history. Every Run requires Research Plan approval before evidence tools execute, and a Task completes only when the Owner explicitly accepts a Report, trading conversational convenience for reproducibility, recovery, and auditability.
